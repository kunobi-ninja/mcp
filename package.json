{
  "name": "@kunobi/mcp",
  "version": "0.0.1",
  "description": "MCP server for Kunobi — platform management for AI assistants",
  "type": "module",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kunobi-ninja/mcp.git"
  },
  "main": "dist/server.js",
  "bin": {
    "kunobi-mcp": "dist/server.js"
  },
  "files": [
    "dist",
    "LICENSE"
  ],
  "scripts": {
    "build": "rm -rf dist && rollup -c && chmod +x dist/server.js",
    "typecheck": "tsc --noEmit",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "check": "biome check .",
    "test": "vitest run --coverage",
    "dev:link": "pnpm link ../mcp-bundler && pnpm link ../mcp-installer",
    "dev:unlink": "pnpm unlink @kunobi/mcp-bundler && pnpm unlink @kunobi/mcp-installer && pnpm install"
  },
  "dependencies": {
    "@kunobi/mcp-bundler": "^0.0.5",
    "@kunobi/mcp-installer": "^0.0.5",
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.14",
    "@types/node": "^24.10.11",
    "@vitest/coverage-v8": "^4.0.18",
    "@rollup/plugin-commonjs": "^29.0.0",
    "@rollup/plugin-node-resolve": "^16.0.3",
    "@rollup/plugin-typescript": "^12.3.0",
    "rollup": "^4.57.1",
    "tslib": "^2.8.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
